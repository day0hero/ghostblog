apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghostblog-volume
  namespace: ghostblog
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: do-block-storage
